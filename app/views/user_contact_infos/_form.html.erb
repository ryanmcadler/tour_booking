<%= form_for(user_contact_info) do |f| %>
  <% if user_contact_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_contact_info.errors.count, "error") %> prohibited this user_contact_info from being saved:</h2>

      <ul>
      <% user_contact_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :first %>
    <%= f.text_field :first %>
  </div>

  <div class="field">
    <%= f.label :last %>
    <%= f.text_field :last %>
  </div>

  <div class="field">
    <%= f.label :phone %>
    <%= f.text_field :phone %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
